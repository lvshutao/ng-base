import { MobileAsyncDirective } from './mobile-async.directive';

describe('MobileAsyncDirective', () => {
  it('should create an instance', () => {
    const directive = new MobileAsyncDirective();
    expect(directive).toBeTruthy();
  });
});

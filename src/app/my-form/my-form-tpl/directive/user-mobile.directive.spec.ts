import { UserMobileDirective } from './user-mobile.directive';

describe('UserMobileDirective', () => {
  it('should create an instance', () => {
    const directive = new UserMobileDirective();
    expect(directive).toBeTruthy();
  });
});
